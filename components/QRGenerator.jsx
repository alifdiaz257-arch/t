'use client';import {useState} from 'react';import QRCode from 'qrcode';
export default function QRGenerator(){const [text,setText]=useState('');const [src,setSrc]=useState('');const go=async()=>setSrc(await QRCode.toDataURL(text||'BlueTools',{width:260,margin:2}));return <><input className="input" value={text} onChange={e=>setText(e.target.value)} placeholder="Teks / URL..."/><button className="btn" onClick={go}>Generate QR</button>{src&&<div style={{textAlign:'center',marginTop:15}}><img src={src} alt="QR" style={{width:260,maxWidth:'100%',background:'#fff',padding:10,borderRadius:10}}/></div>}</>}
